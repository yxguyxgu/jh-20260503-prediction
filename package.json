{
  "name": "prediction-monorepo",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "vercel-build": "prisma migrate deploy --schema=server/prisma/schema.prisma && prisma generate --schema=server/prisma/schema.prisma && npm run build --workspace=prediction-market-client"
  },
  "devDependencies": {
    "prisma": "^6.8.2"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3"
  }
}
